#include "config.h"
#include "Allocator.h"
//template <typename T, int size> void Allocator<T, size>::AddItem(T& item, int id) {
//	allocatedSpace->emplace(id, item);
//}
//
//template <typename T, int size> void Allocator<T, size>::RemoveItem(int i) {
//	allocatedSpace->erase(i);
//}